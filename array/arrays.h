#include<stdio.h>
#include<stdlib.h>


float* createarray(int);
float* assignarray(float*, int);
void printarray(float*, int);
float average(float*, int);
float* oparith(float*, int, char, float);

float** creatematrix(int, int);
float* assignmatrix(float**, int, int);
