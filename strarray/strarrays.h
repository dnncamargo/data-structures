#include<stdio.h>
#include<stdlib.h>

/* Arquivo header para declaração de funções */

int invert(char*, int);
char* caps(char*);
char* concat(char*, char*);
char* remchar(char*, char);
char* trim(char*);
int strlen(char*);
